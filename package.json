{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.html",
    "clean": "rm -rf dist && echo 'Done.'",
    "build": "bun build src/index.html --outdir ./dist && echo 'Done.'",
    "preview": "bun dist/index.html"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "@types/bun": "latest"
  },
  "module": "index.ts"
}
