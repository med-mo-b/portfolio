{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "clean": "rm -rf dist && echo 'Done.'",
    "build": "bun build index.html --outdir ./dist && cp -R public/. dist/ && echo 'Done.'",
    "preview": "bun x serve dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/matter-js": "^0.20.2",
    "@types/node": "latest",
    "typescript": "latest"
  },
  "module": "index.ts",
  "dependencies": {
    "gsap": "^3.14.2",
    "lenis": "^1.3.17",
    "matter-js": "^0.20.0"
  }
}
