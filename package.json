{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "clean": "rm -rf dist && echo 'Done.'",
    "build": "bun build src/index.html --outdir ./dist && cp -r public dist/ && echo 'Done.'",
    "preview": "bun dist/index.html"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "@types/bun": "latest"
  },
  "module": "index.ts"
}
